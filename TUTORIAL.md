# Antigravity CLI エージェント開発ワークショップ：日程調整アプリ編
最終更新: 2026-05-13
目的: 「Crawl」から「Run」へのエージェント進化を体系的に学習する

## 概要
Cloud Shell 上で Antigravity CLI を使用し、対話によるルール抽出（Crawl）から、スキルによるタスクの自動化（Walk）、そしてサブエージェントへの包括的なタスク委任（Run）に至るまでのプロセスを学習します。

**ご注意：**
本リポジトリはハンズオンの教材として作成されており、初期状態のソースコードには意図的に適切ではない記載を含んでいます。
本コードをそのまま本番環境（インターネット上に公開される状態）にデプロイして利用しないでください。

2026/6 現在、モデルは `gemini-3.5-flash` を利用します。
---

## 1. ディレクトリ構成

### ワークショップ開始時
初期状態のアプリケーション構成です。

```text
/home/$USER/schedule-workshop/
└── schedule-app/                # 教材アプリケーション
    ├── app.py
    ├── database.py
    ├── models.py
    ├── docs/
    │   └── python.md            # スタイルガイド（知識源）
    └── README.md
```

### ワークショップ完了時
すべての演習を完了すると、Antigravity CLI の拡張機能を取り入れた以下の構成になります。

```text
/home/$USER/schedule-workshop/
└── schedule-app/
    ├── app.py
    ├── ...
    ├── AGENTS.md                # Step 1 で作成（プロジェクト固有ルール）
    └── .agents/                 # Step 3, 4, 5 で作成（エージェントの拡張定義）
        ├── skills/              # カスタムスキル
        │   ├── python-reviewer/
        │   └── python-autofix/
        └── agents/              # サブエージェント
            └── senior-reviewer.md
```

---

## 2. ワークショップの進め方

### Step 0: 事前準備 - Antigravity CLI のインストール
ワークショップを開始する前に、以下の手順で Antigravity CLI をインストールし、動作確認を行います。

1. **インストールスクリプトの実行**:
ターミナルで以下のコマンドを実行し、Antigravity CLI をインストールします。
```bash
curl -fsSL https://antigravity.google/cli/install.sh | bash
```

2. **インストールの確認**:
正しくインストールされ、実行可能ファイルにパスが通っているかを確認します。
```bash
which agy
```
パス（例: `~/.local/bin/agy` など）が返ってくれば成功です。

3. **API の有効化**:
Antigravity CLI の利用には AI platform の API の有効化が必要になるため、実施します。
```bash
gcloud services enable aiplatform.googleapis.com
```

### Step 1: Crawl - 対話を通じたルールの発見と明文化
最初のステップでは、Antigravity CLI を対話アシスタントとして活用し、プロジェクトの現状を分析します。さらに対話を通じて特定した課題を `AGENTS.md` に記録することで、エージェントにプロジェクト固有のルールを適応させます。

#### 1-1. **リポジトリの理解**:
まずは、アプリケーションがあるディレクトリへ移動しましょう。
以降、Cloud Shell を閉じてしまったり終了してしまった場合も、このディレクトリに移動してから Antigravity CLI を起動してください。
```text
cd ~/gemini-cli-workshop-10x/schedule-app
```

ターミナルで `agy` コマンドを実行し、Antigravity CLI のインタラクティブセッションを起動します。

```text
agy
```

初めて Antigravity CLI を起動すると初回設定が必要です。初回設定は[ラボページ](https://explore.qwiklabs.com/focuses/111375)を参考にしてください。
概要は以下です。

1. ログイン方式を選ぶ（個人アカウントなら Google OAuth, 企業アカウントなら Use a Google CLoud project を選択）
2. 表示された URL を開く
3. Google アカウントでログイン
4. ログイン後表示されたコードを Antigravity CLI に貼り付ける
5. Google Cloud プロジェクト ID の設定
6. リージョンの設定
7. カラーテーマの設定
8. 利用規約への同意


以降の手順はすべて Angtigravity CLI のセッション内で実施します。
まずはエージェントにコードベース全体の概要を質問し、プロジェクトの構成を把握させましょう。

```text
このリポジトリについて教えてください
```

- *解説*: エージェントはカレントディレクトリのファイルを走査し、本アプリケーションが Flask と Firestore をベースに構成されていることを報告します。

ポイント
- 誤ったプロンプトを実行してしまったら、`Esc` キー、または `Ctrl+C` を実行するとプロンプトの実行を停止できます。

#### 1-2. **エージェントの行動原則の確認**:
Antigravity CLI 上で動作する Gemini に対して、毎回「あなたは Google Cloud の専門家です」というようなプロンプトを与えるのは非常に面倒です。その手間を削減し、ユーザのプロンプト入力の効果を最大限発揮するために、システム指示を与えることができます。システム指示は `AGENTS.md` に記載されています。

```text
AGENTS.md の内容について教えてください
```

#### 1-3. **初期レビュー**:
次に、セキュリティの観点からコードの実装状況に対するレビューを依頼します。

```text
本リポジトリのアプリケーションのセキュリティ上の懸念を指摘してください
```

- *期待される結果*: パスワードが4桁の数字のみで構成されていることや、削除エンドポイントにおける認証の欠如など、具体的なセキュリティリスクが提示されます。

#### 1-4. **ベストプラクティスの相談**:
問題を発見した後は、すぐに修正へ移るのではなく、エージェントに「どうすべきか」の知見を求めます。

```text
指摘された内容を参考に、Flask/python のベストプラクティスを教えてください
```

- *期待される結果*: セッション (`flask.session`) を用いた状態管理や、レートリミットの導入など、Flask に適した標準的な解決策が提示されます。エージェントが単なる指摘ツールではなく、技術的な相談役として機能することを体験します。

#### 1-5. **やり取りの抽象化と構造化**:
今までやり取りした内容を、特定のプロジェクトに依存しない、より一般的かつ再利用可能な知見としてまとめてもらいます。

```text
今までやり取りした内容を一般的な開発で使えるナレッジとしてまとめてください
```

- *期待される結果*: セキュリティやベストプラクティスに関する議論が、箇条書きやチェックリストなどの構造化された形式で整理されます。これにより、次のステップで作成する `AGENTS.md` が単なる会話の記録ではなく、洗練された「規約」として機能するようになります。

#### 1-6. **チームポリシーの追加**:
自動化されたナレッジに加えて、人間が定めた独自のチームポリシーを追加で組み込みます。

```text
上記のナレッジに、以下のチームナレッジを追加します。
「パスワードを設定する場合、英数字の場合は 4 桁以上、数値の場合は 6 桁以上とするようにしてください」
```

- *解説*: この手順を挟むことで、後で `AGENTS.md` を再読み込みした際に、AIが一般的なベストプラクティスだけでなく、この独自の「チーム規約」も厳格に守って指摘をしてくれる効果を体感できます。

#### 1-7. **ルールの自動生成と適用**:
同一の指摘を反復させないため、抽象化したナレッジおよび追加したチームポリシーをプロジェクトのローカルルールとして定義します。

```text
上記のナレッジを今後も利用できるように、AGENTS.md に追記してください
```

- *解説*: エージェントは文脈を理解し、自律的に `AGENTS.md` というファイルを作成（または更新）し、合意した「ベストプラクティス」と「チームポリシー」をプロジェクトのルールとして固定してくれます。

#### 1-8. **ルールの再読み込みと適用確認**:
定義したルールをエージェントに再認識させるため、コンテキストのリロードを行います。
`/quit` と入力して現在のセッションを終了し、再度 `agy` コマンドで起動し直してください。（※または、セッション内で `/clear` コマンドを実行することでも再読み込みが可能です）

再起動後、新しく追加したチームルールが認識されているか、直接質問して確認します。

```text
パスワードの設定ルールについて教えて
```

- *期待される結果*: 手順 1-6 で追加した「英数字の場合は4桁以上、数値の場合は6桁以上」という独自のルールがエージェントから回答されます。これでルールが定着したことが確認できました。

ポイント
- `/context` コマンドを入力すると、現在 Antigravity CLI が読み込んでいるコンテキスト情報を参照できます。`Esc` キーで抜けられます。
- `/clear` コマンドは、Antigravity CLI が読み込んでいるコンテキスト（`AGENTS.md` 等の事前情報、ユーザーからのインプット、CLI からの返答）を消去するコマンドです。新しい作業を開始したい場合に利用します。
- `/quit` は Antigravity CLI を終了するコマンドです。終了時の情報を復元できるコマンドが表示されているので、前の状態から始めたい場合は `agy --conversation=<会話ID>` と入力します。


#### 1-9. **ルールの分割と構造化（高度な管理）**:
プロジェクトが成長すると `AGENTS.md` が肥大化してしまいます。これを防ぐため、先ほど追加した「チームナレッジ」だけを別ファイルに切り出し、大元のファイルからリンク（参照）させるようにエージェントに指示します。

```text
パスワードについてのチームナレッジを myteam/team-rules.md に切り出して、AGENTS.md から参照できるようにしてください。
ディレクトリやファイルが無い場合は作成してください。
チームナレッジはチームによって異なるため、AGENTS.md から今回のチームナレッジに関する内容は全て削除し、ファイルを参照する形式にしてください。
myteam ディレクトリや myteam/team-rules.md ファイルが無い場合はチームナレッジが存在しないとみなしてください
```

- *解説*: エージェントは自律的に `myteam` フォルダとファイルを作成し、チームルールをそちらに移動します。同時に、`AGENTS.md` には `チームルールについては myteam/team-rules.md を参照` といったリンクを自動的に追記し、知識の構造化を行ってくれます。

#### 1-10. **参照リンク経由での知識引き出しの確認**:
ファイルを分割しても、エージェントがリンクを辿って正しく知識を引き出せるかを確認します。
手順 1-8 と同様に、Antigravity CLI を再起動するか、`/clear` を実行してコンテキストを削除した後、再度同じ質問をします。

```text
パスワードの設定ルールについて教えて
```

- *期待される結果*: ルールが別ファイルに移動していても、エージェントは `AGENTS.md` からの参照を辿り、正確にパスワードルールの内容を回答します。これにより、実務スケールでのモジュール化されたナレッジ管理手法（Crawlフェーズの完成）を体感できます。

---

### Step 2: Practice - セキュリティ機能の実装 (レートリミット)
ルールの適用後、実際にコードの改修を行います。ここでは、エージェントに安全にコードを改修させるための「Plan モード」の作法を学びます。

#### 2-1. **Plan モードの開始**: 
いきなりコードを書き換えるのはリスクが伴うため、まずは計画専用のモードに入ります。

```text
plan モードに入ってください
```
#### 2-2. **実装方針の策定**:
要求仕様を提示し、実装アプローチを策定させます。

```text
イベントの削除パスワードを、数値 4 桁から数字 6 桁に変更する変更を提案してください
```

- *期待される結果*: エージェントはコードの直接変更を行わず、具体的な実装計画を提示します。

#### 2-3. **実装プランの確認**:

プランの内容を確認したい場合は、以下のように尋ねてみましょう。

```text
プランの内容を教えてください
```

ポイント
- `/artifact` コマンドを実行することでもプランを確認できます。エディタは `Esc` キーで閉じることができます。

#### 2-4. **計画のレビューと軌道修正**:
提示された計画をそのまま実行することもできますが、仕様が変更されることもあるでしょう。その際は、エージェントに再度修正プランを伝えることができます。コードを変更せずに対応を行うことができるため、ソースコード汚染のリスクが低下します。

今回は、以下の修正プランを伝えてみます。

```text
改めて検討したところ、本システムではパスワードは 8 桁とすることが決まりました。
プランを修正してください。
```

- *解説*: AI が書いた「コード」を後から直すより、「計画」の段階で仕様の軌道修正を行う方が安全で手戻りが少なくなります。

再度プランの内容を確認し、パスワードが 8 桁として実装するプランになっていることを確認しましょう。

```text
プランの内容を教えてください
```

ポイント
- 今回の依頼で、エージェントがパスワード変更以外の対応（テストの追加やレートリミットの設定）を提案してくることがあります。本ワークショップではこのまま進めてもよいですし、「今回はパスワードの変更だけにしてください」といったインタラクティブなやり取りを行うことでプランの修正を行っていただいても大丈夫です。

#### 2-5. **実装プランの実行**:
作成されたプランに従って、実際のコード修正を実行させます。

```text
実装プランに沿って実装を行ってください
```

- *解説*: Plan モードを抜けた後、エージェントは自律的に `app.py` などを開き、合意したプラン通りにコードを書き換えてくれます。`automatically accept edits` モードでは変更が自動的に行われ、`manually accept edits` の場合は変更のたびに確認依頼が行われます。

#### 実行中の確認
Antigravity CLI の処理中、コマンドの実行確認を求めてくることがあります。（選択肢は場合によって異なります）以下のケースの場合について、それぞれの意味を示します。

1. 処理を承認する
2. `uv` から始まるコマンドはこの会話中は常に承認する
3. `uv` から始まるコマンドを常に許可するよう設定する
4. 許可しない

```text
Do you want to proceed?
> 1. Yes
  2. Yes, and always allow in this conversation for commands that start with 'uv'
  3. Yes, and always allow for commands that start with 'uv' (Persist to settings.json)
  4. No
```

安全を重視する場合は `1. Yes` を選んでおくのが無難です。どのような作業が行われているかをある程度把握できている場合は `2` を選んでいただいても問題ありません。`3` はこの会話以外でも許可が行われるので、慎重に実施します。

破壊的なコマンドの実行については、都度確認するようにすることを推奨します。例えば `ls` や `cat` コマンドはファイルの変更を行わないので、常に許可してもリスクは少ないと言えますが、`python` や `bash` コマンドはファイルを変更する可能性が高いので確認をしたほうが安全です。

明らかに CLI が想定外の作業を実施しているようであれば、 `4. No` を選んで停止することもできます。


#### 2-6. **動作確認**:
修正が完了できたら、動作確認を行ってみましょう。
Antigravity CLI に以下のように指示します。
```
動作確認を行います。.env から起動に必要な環境変数を読み込んで、サーバーを起動してください
```

- *期待される結果*: 5回目の試行失敗時にロックアウト処理が実行され、適切なエラーメッセージが返却されることを確認します。この際、事前の環境構築が必要になりますので、`README.md` を参照して環境構築を行っておいてください。

無事動作確認が完了したら、サーバーの停止を指示します。
```
動作確認ができました。サーバーを停止してください
```

#### エラーが発生したら
修正内容が誤っていた場合、エージェントにトラブルシューティングを行ってもらいましょう。発生したエラーメッセージをチャットウィンドウに貼り付けることで、エージェントが自律的に課題を解決してくれます。
- *注意*: 生成 AI モデルは確率的に動作するので、何度繰り返しても失敗し続ける可能性があります。その際は「実装を元に戻して」と依頼したうえで、再度コードの修正依頼を行うことで解消する可能性があります。

例えばサーバー側では 8 桁のパスワードを求めているが、Webページ上からは 4 桁の入力しかできない（フロントエンドの修正漏れ）があったとした場合、以下のように指摘してみましょう。修正が行われます。

```text
Webページ上 からパスワードが 4 桁しか入力できません。
```

---

### Step 3: Walk - 自動化されたスキルへの進化
`AGENTS.md` は有用ですが、すべてのルールを単一ファイルに記述すると管理が煩雑になります。「スキル」機能を用いることで知識をモジュール化し、自動発動する仕組みを構築します（Walk段階）。ここでは、チームのスタイルガイドに準拠した独自のレビュー機能を作成します。

#### 3-1. **現状の動作の確認（スキル適用前）**:
まず、専用のスキルを持たない通常の状態でコードレビューを依頼し、その結果を確認します。

```text
app.py を レビューして、問題を簡潔に指摘してください
```

- *解説*: エージェントは一般的な Python のベストプラクティスに基づいて回答しますが、プロジェクト独自の詳細なルール（例: 1行の文字数制限など）までは踏み込めない、あるいは一般的な回答に留まることを確認します。

- *補足*: レビューしてください、という命令のみだとコードベースのサンプルを提示したりと Output が重厚長大になるリスクがあります。その場合は「簡潔に指摘」「xx文字程度」など分量を制限する指示が有効です。毎回指示を有効にしたい場合は `AGENTS.md` に記載するとよいでしょう。

#### 3-2. **スキルの定義作成**:
続いて、詳細なコードレビューを行うことができる仕組みを導入します。
ここで、レビューを行うスキルを作成してみましょう。
スキルの動作に必要を指定することで、スキルの作成、導入はエージェントに委任することが可能です。
以下のプロンプトを実行し、`python-reviewer` スキルを作成してみましょう。

```text
docs/python.md のコーディング規約を元にコードレビューを行うスキルを作成してください。
スキル名は python-reviewer です。
Antigravity CLI のスキルの仕様は docs/skills.md に記載があります。
作成後、適切なディレクトリに配置してください
```

- *注意*: スキルの作成中に、エージェントがコマンドを実行するためにシェル画面へのフォーカスを求めてくる場合があります。その際は `Tab` キーを押してフォーカスを切り替えてください。
- *解説*: エージェントが自律的に `.agents/skills/python-reviewer/SKILL.md` にスキルを作成、保存します。
- *補足*: `docs/skills.md` を指定しているのは、Antigravity CLI 自体が新しい仕組みであり、モデルが Antigravity CLI に関する知識を持っていない可能性があるためです（いわゆる RAG ナレッジとして利用）。

#### 3-3. **トリガー条件の設定**:
スキルを自動的に呼び出すための設定を `AGENTS.md` に追加します。

```text
AGENTS.md に「コードのレビュー依頼があった場合は、必ず python-reviewer スキルを活用して実施してください」というルールを追記してください
```

- *解説*: エージェントがプロジェクトの基本ルールである `AGENTS.md` を更新します。これにより、レビュー要求時に該当スキルが自動的にロードされるようになります。

#### 3-4. **スキルの有効化**:
新しく作成・インストールしたスキルを CLI に認識させるために、再読み込みを実行します。（本手順は不要な可能性もあります）

```text
/plugins reload
```

#### 3-5. **スキルのロード確認**:
以下のコマンドを実行し、作成したスキルが CLI に認識されているかを確認します。

```text
/skills
```

- *期待される結果*: 利用可能なスキルのリストに `python-reviewer` が表示されます。コマンドライン以下のように表示されていれば成功です。`Esc` キーを押してスキル一覧表示モードから抜けましょう。

```text
Workspace skills · Workspace config
  python-reviewer: Google Python Style Guide (doc ...
```

#### 3-6. **スキルの実行テスト**:
作成したスキルを用いたレビューを実施します。

```text
app.py を レビューして、問題を簡潔に指摘してください
```

- *解説*: レビュー要求に基づき、エージェントは裏側で `python-reviewer` スキルを起動し、`docs/python.md` の内容を読み込んだ上で評価結果を返します。先ほどのレビューでは行われなかったより詳細な指摘（例：インポートの形式、Docstring の欠如）が行われることを確認します。

#### 3-7. **独自ポリシーの変更適用**:
スキルを更新してみましょう。ここでは（あまり実用的ではないですが）行数最大長判定を 60 文字に変更してみます。
```
python-reviewer スキルの「行数最大長判定」を 80 文字から 60 文字に変更してください
```

変更後、`/plugins reload` を実行し、再度レビューを依頼してみます。（本手順は不要な可能性もあります）

```
app.py を レビューしてください
```

- *期待される結果*: 新しく設定されたルールに基づき、「1行が60文字を超えている」という厳格な指摘が行われることを確認します。

動作確認が終わったら、スキルの動作を元に戻しておきましょう。

```text
80 文字に戻してください
```

- *補足*: `/clear` 等でコンテキストを消去していない場合、上記のような曖昧な命令でも「ルールを元に戻すのだな」と CLI が解釈してくれる可能性が高いです。一度ターミナルを終了した場合は、`python-reviewer スキルの「行数最大長判定」を 60 文字から 80 文字に変更してください` のような具体的な指示が必要になります。

---

### Step 4: Advanced Walk - スクリプト連携による自動修正
エージェントはテキスト生成に留まらず、外部ツールを実行する能力を持っています。ここでは、標準的なツール（`flake8`, `isort`）をエージェントから操作し、コードを自動修正するスキルを構築します。

#### 4-1. **自動修正スキルの構築**:
スクリプトの作成から権限設定までを エージェントに実行させます。

```text
Pythonコードの自動修正スキルである python-autofix を作成してください。
scripts/autofix.sh に isort と flake8 を実行するシェルスクリプトを書いて実行権限を付与し、
判定にはそのスクリプトを使ってコードを修正するよう指示を書いてください
```

- *解説*: 指示に基づき、エージェントはファイルの作成や `chmod +x` による実行権限の付与を自律的に行います。

#### 4-2. **スキルの再読み込みとリスト確認**:
新しく追加した `python-autofix` スキルを読み込ませるため、再度スキルのリロードを行います。

```text
/plugins reload
```

その後、リストを表示し、複数のスキルが有効になっていることを確認します。

```text
/skills
```

- *期待される結果*: `python-reviewer` と `python-autofix` の複数のスキルが表示されることを確認します。

出力例:

```
Workspace skills · Workspace config
  python-autofix: scripts/autofix.sh スクリプトを実行...
  python-reviewer: Google Python Style Guide (docs/python.md) ...
```

#### 4-3. **自動修正の実行**:
準備したスキルを使用してコードの修正を実行させます。AGENTS.md にスキルの利用を明示的に指定しなくても、プロンプトの内容から「どのスキルを使ったら良いか」を判断してくれますが、`@` を入力して、明示的にスキルを指定することもできます。

```text
@.agents/skills/python-autofix app.py のインポート順とスタイルを自動修正してください
```

- *期待される結果*: エージェントは要求に従い `autofix.sh` を実行します。`isort` でのインポート整理に加え、`flake8` が出力した解析結果を読み取り、自律的にコードを編集してフォーマット違反を修正する一連の流れが確認できます。

---

### Step 5: Run - サブエージェントへのタスク委任
最終フェーズでは、定義した複数のスキル群を統合して利用する「サブエージェント（専門特化エージェント）」を構成し、より複雑なタスクを包括的に委任する方法を学習します。

#### 5-1. **サブエージェントの定義**:
サブエージェントの定義ファイルを作成させます。必要な役割と利用するスキルを自然言語で指定します。

```text
senior-reviewer という名前のサブエージェントを作成し、ファイルに保存してください。
サブエージェントの作成方法（YAMLの書き方など）及び保存方法については docs/subagents.md を参考にしてください。
=====
本エージェントは「Pythonのシニアレビュアー」として、以下の役割・タスクを与えてください。

## レビューワークフロー（以下の手順で実行すること）
1. `python-reviewer` および `python-autofix` スキルを順に実行（活用）し、プロジェクト全体のコードがスタイルガイドに準拠しているかを確認する。
2. 続いて、コード全体を横断的に分析し、セキュリティ上の欠陥や脆弱性がないかを確認する。
3. 上記1、2の確認結果を元に、修正案をまとめた「レビューレポート」を作成する。
4. 作成したレポートを `reports` ディレクトリ以下に保存する。ファイル名は、レビューを実行した時点のタイムスタンプを元に `YYYYMMDD_HHMMSS.md` 形式とすること。
5. レポートの保存後、ユーザーに対し以下の3点を含む「エグゼクティブ・サマリー」を提示して判断を仰ぐこと。
    - **修正の緊急度と影響度**: なぜこの修正が必要か、放置した場合のリスクは何か。
    - **リスクアセスメント**: この修正によって既存機能が壊れる（デグレ）可能性の有無。
    - **人間への確認項目（チェックリスト）**: 修正を承認する前に、ユーザーが特に自分の目で確認すべき箇所。
6. ユーザーから「承認」「条件付き承認（要修正）」「却下」のいずれかの明確な意思表示があるまで、次のアクションに移らないこと。

## 禁止事項・制約条件（絶対に遵守すること）
* 実際のソースコードをエージェント自身の判断で直接修正・書き換えをしてはいけません。本エージェントの役割は「レビューと提案」のみです。
* ユーザーから明示的な「修正実行の指示（承認）」を得るまでは、ソースコードへの変更は一切行わないでください。
```

- *解説*: エージェントは指定された `docs/subagents.md` の仕様を解析し、適切な構造（YAMLフロントマター等）を持った `.agents/agents/senior-reviewer.md` を作成します。

#### 5-2. **サブエージェントの有効化**:

作成が完了したら、以下のプロンプトで作成完了確認を行います。

```text
利用可能なサブエージェントを教えてください
```

#### 5-3. **サブエージェントへの委任**:
メインセッションから `@` 構文を用いて、作成したサブエージェントを呼び出します。

`@` を押すと、先ほど作成したサブエージェントを示す `@.agents/agents/senior-reviewer.md` が選択できるようになっています。

以下のプロンプトを実行して、ファイルを変更せず修正案が提案されることを確認しましょう。

```text
@.agents/agents/senior-reviewer.md プロジェクト全体のコードを横断的にレビューして、修正案を提案してください
```

実行が開始したら、`/agents` コマンドを実行することでサブエージェントがバックグラウンドで実行されていることを確認してください。

以下のような表示ができてきていれば、無事サブエージェントが動作しています。`Esc` キーで抜けます。
```
> ▾ @.agents/agents/senior-reviewer.md プロジェク...  (1 running, 0 done)
    |_ ● Agent(senior-reviewer)  File view · 22s [view details]
```

サブエージェントはバックグラウンドで動いているので、動作中も以下のようにその他のプロンプトを入力することができます。

```text
状況はどうですか？
```
完了したら、レポートが表示されます。また、`reports` ディレクトリ以下にレポートが保存されていますので、内容を確認してみましょう。

#### 5-4. **開発プロセスへの組み込み**:

レビューを実施する際に、毎回サブエージェントを呼び出せるようにしましょう。まずは AGENTS.md に指示を追加します。

```text
レビューを依頼した際、senior-reviewer サブエージェントが起動するように AGENTS.md に追記してください
```

上記に加えて、起動時にサブエージェントが認識されるよう指示を行います。Angigravity CLI ではセキュリティ上の理由で自動的にファイルを読み込むことはしませんが、本指示を与えることで毎回サブエージェントを明示的に指定する必要がなくなります。

```text
起動時、.agents/agents/ 以下に定義されたマークダウンファイルからサブエージェントを読み込み、利用可能とできるよう AGENTS.md に追記してください。
```

#### 5-5. **レビューエージェントの実行**:
CLI を一度再起動して、レビュー依頼を行ってみましょう。

```
/quit
```

`agy` コマンドで再起動を行ったら、以下のプロンプトを実行します。

```
定義されているサブエージェントについて教えてください
```

`senior-reviewer` が表示されれば成功です。

#### 5-6. **開発エージェントの実行**:

今回レビューした内容を修正してくれるサブエージェントも作ってみましょう。

```text
senior-developer という名前のサブエージェントを作成し、ファイルに保存してください。
サブエージェントの作成方法（YAMLの書き方など）及び保存方法については docs/subagents.md を参考にしてください。
=====
本エージェントは「Pythonのシニアエンジニア」として、以下の役割・タスクを与えてください。

## 開発ワークフロー（以下の手順で実行すること）
1. 開発内容を reports ディレクトリ以下にあるマークダウンファイルから取得する。その際、ユーザーに「どのマークダウンファイルを読み込むか」を確認し、承認を得ること。
2. 読み込んだ内容を精査し、実装を開始する前に「実装計画のブリーフィング」をユーザーに提示して承認を得ること。
    - 変更対象となる具体的な関数・ファイル名。
    - どのようなテストケースを追加/修正して、品質を担保する予定か。
3. 実装完了後、必要なテストコードを自ら生成・実行し、すべてのテストがパスすることを確認すること。既存のテストがある場合はそれも実行し、デグレがないことを担保すること。
4. 実装完了後、「実装完了報告」を提示し、人間の最終確認を求めること。
    - **テスト実行結果のエビデンス**: 実際にパスしたテストのログ詳細（成功した数、失敗した数、具体的な実行コマンド）。
    - 実際にパスしたテスト結果のサマリー。
    - `dev_logs` に記録した内容の要約。
    - **「ここをレビューしてほしい」という自己レビュー箇所**: 実装中に悩んだ部分や、より良い書き方があるかもしれない箇所。
5. 「どのような実装を行ったか」「懸念点はなにか」および「テスト実行エビデンス」を dev_logs ディレクトリに、読み込んだマークダウンファイルと同名で保存すること。

## 開発ルール（常に遵守すること）
* docs/python.md のスタイルガイドに準拠した開発を実施すること。
* シニアエンジニアとしての自覚を持ち、バグが出ないよう注意深く実装を行うこと。
```

`senior-developer` エージェントができたら、開発も委任してみましょう。

まずは修正する内容を決めます。先ほどのレビュー内容を確認してみます。

```
reports 以下にある最新のレビュー結果について教えてください
```

その中で気になった内容について、切り出してエージェントに与えてみましょう。以下の例では、スタイルガイドの違反について切り出しています。

全ての内容を指定することもできますが、意図しない動作でリポジトリが大幅に変更されてしまうリスクを考慮し、変更内容は小さくとどめておくことがおすすめです。

```
スタイルガイド違反について修正したいので、該当の実装プランを切り出してください。
style_guide_fixes.md という名称で dev_plans ディレクトリ以下に作成してください。
```

ファイル名が特定できたら、エージェントへ依頼を行いましょう。

```
@.agents/agents/senior-developer.md style_guide_fixes.md に沿って実装を行ってください
```

上記を実行することで実装プランが提示されます。「承認します」という旨を伝えることで、実装が始まります。

また、以下のように実装をしたい旨だけを伝えた場合、どのマークダウンを使うか？という形で聞き直してくれます。
```
@.agents/agents/senior-developer.md 実装を実施したいです。
```

このように、メインエージェントがサブエージェントとやり取りを行い、実装を進めていくという流れができました。

最後に、開発についてもサブエージェントが起動するように AGENTS.md に追記しましょう。

```text
開発、修正を依頼した際、senior-developer サブエージェントが起動するように AGENTS.md に追記してください
```

これにより、レビュー、開発を全てエージェントへ任せることができるようになりました。

#### サブエージェントとのやり取り
メインエージェントはサブエージェントに実作業を委任しますが、その際サブエージェントが必要な情報を聞いてくることがあります。

その際返答を行うと、メインエージェントとサブエージェントは、｀SendMessage` という命令でやり取りを行います。

```text
> style_fixes_plan.md でお願いします

▸ Thought for 3s, 163 tokens
  Processing User Feedback

● SendMessage(Message sent to "af40e4f6-47ac-4b9c-88ed-92037a3ec3de".) (ctrl+o to expand)
```

また、サブエージェントからコマンドの実行の承認を求められることもあります。書かれているように `Ctrl+K` を入力して承認するか、`alt+j` を押して拒否することができます。

```text
senior-developer needs approval for Bash
 ┃ ──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 ┃
 ┃ Requesting permission for: pytest
 ┃
 ┃ ctrl+k approve · alt+j manage
```

### まとめ

一問一答の対話から、自律稼働するエージェント群による高度な開発支援プロセスへの進化を確認できたでしょうか。
以上で、本ワークショップは完了となります。

---

### コラム: Hook 機能によるガードレールの設置

**Hook** とは、Antigravity CLI がエージェントループ内の特定の時点で実行するスクリプトまたはプログラムのことです。これを利用することで、CLI のソースコードを変更することなく、特定の処理を対話プロセスに挟み込むことができます。

例えば、以下のような活用が可能です：
- **機密情報の検知（ガードレール）**: コード生成やファイル更新のタイミングでスキャナを走らせ、パスワードやシークレットが混入しようとした場合に即座にブロックする。
- **実行前チェック**: LLM がツール（コマンド実行など）を使用する直前に、特定のセキュリティチェックやバリデーションを行う。
- **セッション開始時の環境構築**: セッション開始時に自動的に必要な環境変数を読み込んだり、依存関係を確認したりする。

このように、Hook を活用することで、開発の初期段階で問題を検知する「シフトレフト」の考え方をエージェントとの協調作業に取り入れ、より安全な開発環境を構築できます。

## References
- [Best practices for Antigravity CLI](https://antigravity.google/docs/cli-best-practices)